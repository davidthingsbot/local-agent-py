[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers -m "not e2e"
markers =
    e2e: end-to-end test that requires real llama-server processes (run via tests/e2e/run.sh)
filterwarnings =
    ignore::DeprecationWarning
